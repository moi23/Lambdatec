import React from "react";
import { Container } from "./styles";
import MBlocoParagraph from "../Util/MBlocoParagraph";

export default function Section9() {
  return (
     <Container>
       <MBlocoParagraph
       text ="O gerenciamento da estrutra é realizado pela equipe da LambdaTec e, caso solicitado,
       por profissionais de TI especializados dentro da empresa do cliente, para que o ambiente seja
       mantido estável e seguro. As configurações e acessos nos servidores são otimizados para que sejam
       feitos através de chamados."
     width="1000px"
     />
    </Container>
  );
}

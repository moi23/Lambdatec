import styled from "styled-components";

export const Container = styled.div`
  @import url("https://fonts.googleapis.com/css?family=Roboto&display=swap");
  /* height: 1080px;
  width: auto;
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;

  video {
    width: 100%;
    position: relative;
    background-attachment: fixed;
    pointer-events: none;
  }

  img {
    position: absolute;
    height: 50%;
    top: 10%;
  }

  button {
    background: transparent;
    color: white;
    border: 2px solid white;
    position: absolute;
    bottom: -10%;
    font-size: 1em;
    height: 60px;
    width: 180px;
    opacity: 40%;
    transition: 0.5s;
    cursor: pointer;
    transform: scale(1);
    font-weight: bold;
  }

  button:hover {
    background: white;
    color: black;
    border: 2px solid white;
    position: absolute;
    bottom: -40px;
    font-size: 1em;
    cursor: pointer;
    z-index: 999;
    transform: scale(1.2);
  }
  .Teste {
    position: absolute;
    color: white;
    bottom: 1px;
    font-size: 3rem;
    letter-spacing: 10px;
  }

  h2 {
    position: absolute;
    color: white;
    bottom: 1px;
    font-size: 3rem;
    letter-spacing: 10px;
  }
  h3 {
    position: absolute;
    bottom: 100px;
    color: white;
  } */

  @media screen and (min-width: 1680) and (max-width: 1920) {
    height: 1080px;
    width: auto;
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;

    video {
      width: 100%;
      position: relative;
      background-attachment: fixed;
      pointer-events: none;
    }

    img {
      position: absolute;
      height: 50%;
      top: 10%;
    }

    button {
      background: transparent;
      color: white;
      border: 2px solid white;
      position: absolute;
      bottom: 3%;
      font-size: 1em;
      height: 60px;
      width: 180px;
      opacity: 40%;
      transition: 0.5s;
      cursor: pointer;
      transform: scale(1);
      font-weight: bold;
    }

    button:hover {
      background: white;
      color: black;
      border: 2px solid white;
      position: absolute;
      bottom: 6%;
      font-size: 1em;
      cursor: pointer;
      z-index: 999;
      transform: scale(1.2);
    }
    .Teste {
      position: absolute;
      color: white;
      bottom: 13%;
      font-size: 3rem;
      letter-spacing: 10px;
    }

    h2 {
      position: absolute;
      color: white;
      bottom: 1px;
      font-size: 3rem;
      letter-spacing: 10px;
    }
    h3 {
      position: absolute;
      bottom: 25%;
      color: white;
    }
  }

  /* TELAS 1920 */
  /* @media (max-width: 1920px) {
  } */

  /* TELAS 1024 */
  /* @media (max-width: 1024) {
    height: 1024px;
    width: auto;
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;

    video {
      width: 100px; */
  /* position: relative; */
  /* background-attachment: fixed; */
  /* pointer-events: none;
    }

    img {
      position: absolute;
      height: 50%;
      top: 10%;
    }

    button {
      background: transparent;
      color: white;
      border: 2px solid white;
      position: absolute;
      bottom: 6%;
      font-size: 1em;
      height: 60px;
      width: 180px;
      opacity: 40%;
      transition: 0.5s;
      cursor: pointer;
      transform: scale(1);
      font-weight: bold;
    }

    button:hover {
      background: white;
      color: black;
      border: 2px solid white;
      position: absolute;
      bottom: 6%;
      font-size: 1em;
      cursor: pointer;
      z-index: 999;
      transform: scale(1.2);
    }
    .Teste {
      position: absolute;
      color: white;
      bottom: 13%;
      font-size: 3rem;
      letter-spacing: 10px;
    }

    h2 {
      position: absolute;
      color: white;
      bottom: 1px;
      font-size: 3rem;
      letter-spacing: 10px;
    }
    h3 {
      position: absolute;
      bottom: 25%;
      color: white;
    }
  } */
`;
